******

### История выпусков

******

# v0.2.2

###### 2026/09/12

* `Исправление` Исправить утечку дескрипторов при экспериментальной перезагрузке watch после ошибки close_range: установить CLOEXEC для реальных FD до exec через существующий ограниченный резервный путь и остановиться при неполной подготовке. Сохранить stdio, явный IPC и обработку сигналов. Проверки полного исходного кода GCC/Clang охватывают реальный exec, высокие FD, сниженный жесткий лимит и внедренные ошибки; новые нативные сборки и неизменные тесты Android регистрируются отдельно. Исторические ошибки, официальные файлы и границы распространения сохранены
* `Исправление` Исправить проверку прежних экспериментальных сборок JSC после обновления исходного кода Bun: привязать полный неизменяемый архив сборки и сохранить строгую проверку текущих входных данных для новых сборок
* `Исправление` Исправлена преждевременная доставка заблокированных ожидающих сигналов при экспериментальном ожидании epoll на Android: сохраняется маска вызывающего потока и действующее исключение epoll_pwait2. Проверки GCC/Clang компилируют исходную и исправленную функцию целиком; новые нативные сборки и неизмененные проверки устройств учитываются отдельно, сохраняя архивы отказов версии с одиннадцатью патчами и границы официальной поддержки. Неизменные наборы тестов дважды пройдены в пяти нативных средах с размером страницы 4 KiB: 330/330 проверок приложения и 80/80 тестов Binder. Два сбоя запуска ART на API 28 сохранены отдельно; третья попытка с теми же APK проходит оба раунда. Проверки нового исходного кода на Samsung ARM64 завершены для этих фиксированных наборов; перенос JSC записан отдельно
* `Исправление` Исторический результат одиннадцати патчей: Исправлена преждевременная доставка ожидающего SIGSYS при экспериментальном Android spawn: родитель сохраняет маску вызывающего потока, обработка при подготовке разрешена только потомку, а при блокировке SIGSYS используется существующий путь присоединения потомка к cgroup. Проверки GCC/Clang охватывают всю рабочую функцию и контрольные отказы старого кода; свидетельства новых сборок и устройств учитываются отдельно от исторической приемки и официальной поддержки. Повторная проверка на устройствах подтверждает сохранение сигнала сразу после spawn, но он по-прежнему доставляется во время асинхронного ожидания; независимое чтение регистров и маски epoll выявляет следующий блокирующий дефект, и полный набор из 33 проверок всё ещё не пройден
* `Исправление` При временном сбое проверки готовности повторять ее по следующему запросу после паузы 30 секунд, объединять одновременные проверки и ограничивать каждый поток вывода 4 KiB. Дополнить локализованные сводки данными API, ABI, этапа, версии среды, кода выхода и повторной попытки; явно отмечать предполагаемые сигналы, сохраняя нативные файлы и границы поддержки
* `Исправление` Исправлено экспериментальное обнаружение pidfd в Android при первом асинхронном spawn с заблокированным SIGSYS: существующий резервный waiter сохраняет маску вызывающего потока и ожидающие сигналы. Runtime с десятью патчами прошел неизмененные 31 пробу (310/310) и все восемь тестов Binder (80/80) в пяти нативных средах 4 KiB. Восстановленные доказательства завершения сборки явно отмечают утрату исходных кодов выхода; первый сбой запуска ART сохранен отдельно. Проверки rebase JSC и Release остаются открытыми; официальные файлы не изменены
* `Исправление` Исправлено аварийное завершение экспериментального JavaScriptCore при запуске на нативном x86_64 с 16 KiB: закрепленный WebKit пересобран с явными настройками больших страниц, JIT и аллокаторов, Bun перелинкован. Два полных прогона Binder на AVD 4 KiB и 16 KiB успешны (32/32). Официальные бинарные файлы и ограничитель 4 KiB неизменны; приемка Release остается отдельной
* `Улучшение` Отдельная фиксация кандидата JSC с большими страницами и тринадцатью патчами: две новые чистые сборки Bun сохраняют фактический код выхода 0, одинаковые полные бинарные файлы и 21 неизменный вход сборки. Повторно используются исходные независимые библиотеки JSC и ICU; новые APK и прежние регрессии Binder/семи режимов нагрузки связываются отдельно, без переноса исторических результатов и расширения выпуска
* `Улучшение` В фиксированную диагностику watch/reload добавлены ограниченные снимки SIGABRT только для чтения с сохранением доставки сигналов и лимитов. Шестнадцать наблюдений с трассировкой и без неё на нативном ARM64 API 28/31 завершили 32 перезагрузки и зафиксировали 24 обычных SIGSYS; пакеты и UID очищены. Причина невоспроизведённого исходного аварийного завершения API 28 остаётся открытой, число проверок совместимости не увеличено
* `Улучшение` Добавить фиксированную регрессию watch/reload в режимах native/TRAP без изменения среды с двенадцатью патчами и исходных 33 проверок. На четырех устройствах ARM 64 / 4 KiB исходные проверки проходят 264/264, но 14/16 новых режимов завершаются неудачей:28 утечек дескрипторов при 32 перезагрузках. Нативный контроль Sony 5.15 проходит, принудительный TRAP не проходит. Сохранить три партии APK с привязкой к исходникам и ранние исправления валидатора, проверить очистку пакетов и UID. Исправление нативного кода, расширенная совместимость и Release остаются открытыми
* `Улучшение` Завершить регрессию двенадцати патчей на Samsung SM-F936U / API 32 / нативном ARM64 / 4 KiB: неизмененные проверки проходят 66/66, полный набор Binder 16/16 за два раунда. Повторно использованы все три APK из серии API 36; проходят 40 контрольных точек ожидающего сигнала и 12 наблюдений дочерних процессов. Все тестовые пакеты удалены, проверено отсутствие процессов трех UID, частный сервер ADB закрыт без управления AVD. Обе проверки Samsung для фиксированных наборов завершены; семь базовых сред дают 462/462 проверок и 112/112 Binder. Расширенное покрытие, нагрузка и Release остаются открытыми
* `Улучшение` Проверить неизмененный runtime с двенадцатью патчами на Samsung SM-A566B / API 36 / нативном ARM64 / аппаратных страницах 16 KiB: два раунда проходят все 33 проверки (66/66) и полный набор Binder (16/16). Четыре режима ожидающего сигнала сохраняют 40 контрольных точек и 12 наблюдений дочерних процессов до однократной доставки каждому вызывающему потоку. Повторно использованы те же APK без нативной пересборки, проверено отсутствие процессов трех UID после удаления и закрыт частный сервер ADB без управления AVD. Шесть базовых сред дают 396/396 проверок и 96/96 Binder; ARM64 API 32, расширенное покрытие и Release остаются открытыми
* `Улучшение` Проверить кандидата JSC с большими страницами и двенадцатью патчами на нативном x86_64 API 36 со страницами приложения 4 KiB и 16 KiB: исходные тесты Binder проходят 32/32, фиксированные режимы нагрузки 28/28 с одной парой APK, привязанной к 83 входным файлам. Сохранить первое завершение сервиса из-за нехватки памяти отдельно; полный повтор с теми же APK проходит без изменения настроек и лимитов. Проверить отсутствие процессов обоих UID после очистки, разделяя эмуляцию страниц x86, базовую проверку Samsung и выпуск
* `Улучшение` Привязать отдельный кандидат JSC с большими страницами и двенадцатью патчами к двум новым одинаковым чистым сборкам Bun, сохранив реальные коды завершения и 21 неизменный вход сборки. Точно повторно использовать исходные независимо собранные библиотеки JSC и ICU, сохранить прежние кандидаты и связать изолированный Binder с новым исходным кодом. Сценарий нагрузки, валидатор и ограничения не меняются; проверки устройств и Release остаются отдельными
* `Улучшение` Добавлены фиксированные проверки spawn с ожидающим SIGSYS и независимая трассировка сигналов. Оба новых режима дважды завершаются ошибкой на нативном ARM64 с API 28/31, при этом все исходные 31 проверки проходят; восемь трасс подтверждают раннюю доставку SI_TKILL. Ошибки, привязки исходников/APK и очистка архивируются без изменения нативных файлов или расширения заявленной совместимости
* `Улучшение` Локализовать сводки ошибок на десять языков, сохранить стабильные коды и ограниченные технические сведения, а помощь по активации, требованиям Android, тайм-аутам и лимиту вывода генерировать из тех же ресурсов Android. Отображать кешированные отказы по размеру страницы на текущем языке и не разрывать символы Unicode при сокращении диагностики
* `Улучшение` Добавить генерируемую матрицу свидетельств совместимости с полным индексом источников, хешами архивов и проверкой рассогласований в CI; сохранить результаты по средам выполнения, устройствам и наборам тестов, включая сбои и предварительную диагностику, без новых приемочных испытаний устройств или расширения официальной поддержки
* `Улучшение` Проверить новый x86-кандидат с большими страницами и десятью патчами на API 36 с пользовательскими страницами 4 KiB и 16 KiB: неизменные тесты Binder прошли 32/32, фиксированные режимы нагрузки 28/28 с одной парой APK, связанной с 77 входами. Сохранить байты, исходные результаты и очистку отдельно от истории; x86 ABI 16 KiB остается эмуляцией поверх страниц ядра 4 KiB, без приемки Release или оценки производительности
* `Улучшение` Отдельно привязать кандидата JSC с большими страницами к десяти патчам, требуя две одинаковые чистые сборки Bun, сохранённые реальные коды завершения и точные повторно используемые входы JSC. Изолированные инструменты Binder используют новую привязку к исходникам; исторические и официальные бинарные файлы сохранены, проверки устройств и Release остаются отдельными
* `Улучшение` Добавить руководство по устранению неполадок на китайском с минимальным скриптом, проверками симптом/причина/действие и ограниченными данными для отчёта; дать ссылки во всех десяти языках README без изменения возможностей runtime
* `Улучшение` Завершена регрессия неизмененного runtime с десятью патчами на Samsung: нативные ARM64 API 32 / 4 KiB и API 36 / 16 KiB прошли по два запуска 31 пробы (62/62) и полного набора из восьми тестов Binder (16/16) с одинаковыми APK на обоих устройствах. Семь нативных сред дают 434/434 пробы и 112/112 Binder. Точно связаны исходники, APK и очистка без пересборки нативного кода или операций с AVD; rebase JSC, расширенная матрица runtime и Release остаются открытыми
* `Улучшение` Добавлен отдельный тестовый наблюдатель SIGSYS: четыре сбоя на нативном ARM64 с API 28 прямо указывают на pidfd_open, контроль без трассировки также завершается сбоем, а API 31 проходит проверки. Привязаны исходные данные, сохранен ранний сбой инструмента, проверены передача сигналов, отклонение подмен и очистка; это диагностика, а не приемка нового runtime, Binder или Release
* `Улучшение` Добавлены две фиксированные проверки асинхронного spawn с заблокированным SIGSYS без изменения исходных 29 определений и нативных байтов: четыре нативных окружения 4 KiB прошли 248/248, но на Sony API 28 оба новых режима дважды завершились с кодом 159 (итого 58/62). Все четыре сбоя сохранены в отдельном строгом архиве, проверка совместимости остается непройденной; тестовые пакеты, процессы UID и AVD этой сессии очищены, новая приемка Binder, 16 KiB и Release не заявляется
* `Улучшение` Завершить проверку четырех режимов жесткого лимита FD на нативном ARM64 16 KiB в Samsung SM-A566B / API 36: тот же APK и неизменные 29 проб проходят два раунда (58/58), включая восемь случаев с успешным нативным close_range и обходным путем после принудительного TRAP. Подтвердить отсутствие процессов UID после удаления пакета без операций с AVD; итог в семи средах 406/406. Без пересборки native, повторного запуска Binder или приемки Release
* `Улучшение` Проверить Samsung Galaxy Z Fold4 SM-F936U с фактическим API 32 / Android 12L, нативным ARM64 и страницами 4 KiB: два раунда существующего набора Binder (16/16) и неизменных 29 проб приложения (58/58) пройдены, включая восемь случаев жесткого лимита. Привязать APK, исходники и сырые данные, удалить три тестовых пакета и подтвердить отсутствие процессов UID без операций с AVD. Нативные байты не изменены; новые случаи на нативном ARM64 16 KiB, расширенная матрица и приемка Release остаются открытыми
* `Улучшение` Добавлены четыре фиксированные проверки жесткого лимита FD Android без изменения исходных 25 определений и байтов runtime: по два раунда 29/29 в пяти нативных средах 4 KiB (290/290), включая 40 проверок жесткого лимита. Проверены CLOEXEC при запуске и оба API spawn после снижения мягкого/жесткого лимитов до 128, лимиты приложения и supervisor не изменены. Исходные данные привязаны, тестовые пакеты и AVD этой сессии очищены; FD 70000, новые проверки нативных 16 KiB и Release остаются открытыми
* `Улучшение` Добавлена отдельная ограниченная нагрузочная проверка JSC на API 36 x86_64: семь автономных режимов дважды проходят с пользовательскими страницами 4 KiB и эмулируемыми 16 KiB (28/28), включая реальные выборки LLInt/Baseline/DFG/FTL, GC, Wasm и 64 штатных завершения worker. Те же APK проходят неизмененный набор Binder (32/32). Отдельно записаны отображения ядра 4 KiB, сохранены предварительные результаты и очищены собственные тестовые устройства; нативные байты, стабильная поддержка и условия Release не изменены
* `Улучшение` Завершена проверка API 29 и 32 на нативном x86_64 / 4 KiB: по два запуска добавили 32/32 теста Binder и 100/100 неизмененных проб приложения, завершив покрытие версий API 28-32 для x86 Binder. Добавлено строгое архивирование исходных отчетов, привязок к исходникам и очистки; прежние данные сохранены, ARM64 API 32 и Release остаются непроверенными, закрыты только два AVD, запущенных для этой проверки
* `Улучшение` Добавлен отдельный необязательный test-only плагин с производственным сервисом и полным набором из 8 Binder-тестов для Bun с девятью патчами: два прогона успешны в девяти нативных средах (144/144), включая ARM64 16 KiB. Сохранены привязки APK к исходникам и доказательства очистки, стабильная поддержка не расширена
* `Улучшение` Добавить фиксированную автономную CLI-пробу внутреннего lchmod/fchmodat2 без изменения бинарных файлов и исходных 24 проб: все 25 проходят дважды в шести нативных средах (300/300), включая ARM64 16 KiB. Проверить права, повторные ссылки, no-follow, игнорируемый EIO и достижимость через SIGSYS с ограниченной очисткой потоков; сохранить ранние сбои тестовой обвязки, удалить тестовые пакеты и закрыть запущенный AVD. Полный экспериментальный Binder и Release остаются непроверенными
* `Улучшение` Проверить экспериментальный Bun с девятью патчами на Samsung с нативным ARM64 / API 36 / 16 KiB, используя тот же APK и неизменные 24 пробы: 48/48 за два прохода, все 48 проверок путей успешны, 12 запросов выхода за корень не читают тестовый маркер; пакет удален, процессов UID не осталось, AVD не запускались и не выключались. Полные проверки экспериментального Binder, Release и x86_64 16 KiB еще открыты
* `Улучшение` Проверка выравнивания страниц 16 KB для 64-битных нативных библиотек при сборке, включая контракт manifest и отчеты JSON
* `Зависимость` Онлайн-плагин platform-versions приведен к закрепленной в репозитории версии 1.7.4 без изменения native-alignment

# v0.2.1

###### 2026/09/10

* `Подсказка` Снимок разработки, ещё не опубликованный; официальный плагин по-прежнему требует Android 13 (API 33) или новее
* `Исправление` Сохранить экспериментальную раздачу статических каталогов без openat2 через закрепленный MIT-резервный путь на основе FD: фиксировать компоненты, разрешать ссылки внутри корня, отклонять внешние и магические ссылки, ограничивать обход и контролировать ошибки и владение FD; обычный доступ Bun.file/node:fs, официальный runtime и минимум Android 13 не меняются
* `Исправление` Исправить резервный путь Linux spawn FD экспериментальной среды закрепленным MIT-патчем: перечислять реальные дескрипторы через прямые syscall и фиксированный буфер стека, учитывать FD выше сниженных мягкого/жесткого лимитов и прежнего потолка 65536, завершать запуск до exec при неполной изоляции; добавить регрессии vfork/exec, ошибок, символов и контроль старой реализации без изменения официального Bun и минимума Android 13
* `Исправление` Исправить резервный путь CLOEXEC при запуске экспериментального Bun закрепленным MIT-патчем: перечислять открытые дескрипторы без потолка их номеров, сохранять fd 0-3 и завершаться при неполной установке флагов; добавить нативные тесты ошибок и границ, отделить проверку входов сборки от приемки runtime, не меняя официальный runtime и минимум Android 13
* `Исправление` Исправить очистку при тайм-ауте, отмене и превышении вывода в официальном плагине с помощью закрепленного супервизора только для чтения: усиливать игнорируемый SIGTERM до SIGKILL, дожидаться завершения непосредственного дочернего Bun и сохранять остаток вывода; Bun 1.4.0 и минимум Android 13 не меняются
* `Исправление` Исправить завершение изолированного пробника patched Bun, компилируя общий SupervisedProcess из исходного файла и включая закрепленный супервизор; связать исходники, инструменты и байты супервизора в записях сборки схемы 2 и не закрывать чтение вывода до завершения
* `Улучшение` Проверить резервное открытие каталогов двумя побитно одинаковыми чистыми сборками на ABI и прежними 24 тестами дважды в пяти нативных средах 4 KiB (240/240): все 60 ранее неуспешных проверок выхода теперь отклоняют метку, обычная раздача работает. GCC/Clang и GCC ASan/UBSan проходят; пакеты и AVD очищены. Прежние сбои сохранены; нативный ARM64 16 KiB, полный Binder и Release требуют отдельной проверки
* `Улучшение` Предыдущая неуспешная база (a260ef308): Добавить 24-ю проверку ограничения путей openat2 без изменения экспериментального runtime: пять нативных сред со страницами 4 KiB дважды дают 23/24. Прежние 230 наблюдений проходят, но 10 новых сбоев фиксируют 60 чтений синтетической метки вне заданного корня через относительные, абсолютные и магические ссылки. Сохранить неуспешную проверку без ослабления утверждений; аварий, зависаний и оставшихся процессов тестового UID нет, запущенный AVD остановлен. Нативное исправление пока не внесено
* `Улучшение` Исправить аудит fchmodat2: внутренний sys::lchmod использует константу SYS_FCHMODAT2 в верхнем регистре, а оба публичных экспорта lchmod в Android node:fs отсутствуют во всех 10 запусках. Внутренний резервный путь создания ссылок на исполняемые файлы пакетов и установка зависимостей не выполнялись; сохранить исторический отчет и блокировку распространения
* `Улучшение` Расширить набор syscall-проверок неизмененного экспериментального runtime до 23: пять нативных сред с размером страницы 4 KiB проходят два запуска (230/230), включая 60 наблюдений TRAP→ENOSYS и 16 проверок резервного копирования/ожидания с контролем EIO; явно исключить четыре случая API 28, ограниченных ядром или политикой, из доказательств достижения ветви, сохранить исходные проверки и историю, удалить тестовые пакеты и завершить запущенный AVD. Полные проверки syscall/Binder и новые нативные тесты 16 KiB остаются открытыми
* `Улучшение` Проверить исправление spawn в шести нативных средах: те же 20 проверок проходят дважды на arm64 API 28/31/33/35 и x86_64 API 33 (4 KiB), а также Samsung arm64 API 36 (16 KiB), всего 240/240; оба ABI воспроизводятся в двух чистых сборках, все 36 случаев принудительного завершения проходят, тестовые пакеты удалены, запущенный в этой сессии AVD остановлен. Прежние ошибки сохранены; полная проверка экспериментального Binder и выпуск еще не завершены
* `Улучшение` Проверить нативное ARM64-исполнение с 16 KiB на Samsung Remote Test Lab SM-A566B (API 36): arm64-only APK разработки v0.2.1 с официальным Bun и зафиксированным supervisor дважды проходит все 8 тестов Binder, включая перезапуск, хеши установленных файлов и 10 случаев принудительного завершения; сохранить привязки исходников/APK/логов и удалить тестовые пакеты, оставив проверки Release и x86_64 отдельными
* `Улучшение` Прежняя версия с ошибками (c240d6c68): Зафиксировать результат неизмененного экспериментального runtime на том же нативном ARM64-устройстве с 16 KiB: дважды 19/20. Нативный close_range, стартовая установка флагов и жизненный цикл проходят, но известная утечка fd при spawn с принудительным TRAP и сниженным RLIMIT_NOFILE остается; сохранить оба сбоя без заявления о полной приемке экспериментального Binder или runtime
* `Улучшение` Прежняя версия с ошибками (c240d6c68): Расширить экспериментальный набор до 20 проверок с понижением RLIMIT_NOFILE: четыре нативных arm64-устройства (API 28/31/33/35) дважды дают 18/20, а нативный x86_64 AVD с API 33 дважды дает 19/20, все со страницами 4 KiB. Все прежние 180 наблюдений по-прежнему успешны; 18 новых сбоев показывают наследование fd 256 обеими API spawn после снижения мягкого лимита до 128. Сохранить ошибки, восстановление лимитов и очистку без изменения runtime и заявления об исправлении дефекта
* `Улучшение` Описать варианты ARM64 с 16 KiB для Windows x64: VMware и WSL сами по себе не дают нативный ARM64 Android. Отдельно обозначить полносистемную программную эмуляцию и предложить удаленные устройства Samsung с 16 KiB через RDB/ADB при подтвержденных доступности и разрешениях, не заявляя о новой приемке на устройствах
* `Улучшение` Прежний результат 18 проверок: Добавить пять проверок FD/SIGSYS и подтвердить исправление запуска: четыре нативных arm64-устройства (API 28/31/33/35) и нативный x86_64 AVD с API 33 дважды проходят 18/18, всего 180/180; две чистые сборки каждого ABI побайтово совпадают. Сохранить прежние отчеты с результатом 17/18, не меняя официальный runtime и минимум Android 13; полный экспериментальный Binder и нативные страницы 16 KB пока не проверены
* `Улучшение` Связать исходники супервизора, инструкции сборки с фиксированным NDK и хеши по ABI в схеме 2 corresponding-source manifest, с точной проверкой файлов исходного архива и без изменения ресурсов v0.2.0
* `Улучшение` Добавлены сборщик изолированного APK только для тестов и запуск на явно указанном устройстве для воспроизводимого patched Bun, с точной проверкой исходников/APK/runtime, временной тестовой подписью и ограниченными машиночитаемыми отчётами
* `Улучшение` Все 13 проверок процесса приложения пройдены дважды на нативных arm64-устройствах с API 28, 31, 33 и 35: 24 случая игнорирования SIGTERM при тайм-ауте, лимите вывода и отмене после готовности подтверждают выход дочернего процесса и супервизора и очистку каталога; исходный отчет о неудаче 10/12 сохранен без заявлений о полном экспериментальном Binder или расширении поддержки Android
* `Улучшение` Сохранены свидетельства проверки опубликованных APK и исходников v0.2.0 и приёмки подписанных пакетов на устройствах, без изменения опубликованного тега и расширения совместимости Android/16 KB
* `Зависимость` Обновление публикуемого онлайн плагина сборки autojs6-platform-versions с 1.7.3 до 1.7.4 и синхронизация требований к версии в репозитории

# v0.2.0

###### 2026/09/08

* `Подсказка` Этот выпуск снижает минимальное системное требование с Android 14 до Android 13 (API 33); Android 9 по 12L (API 28 по 32) остаются неподдерживаемыми, пока пропатченный Bun runtime не пройдет проверку переносимости
* `Исправление` Усилить проверку файлов Release: сверять размер и SHA-256 архива WebKit непосредственно с зафиксированными значениями и поддерживать разные форматы вывода apksigner
* `Исправление` Определять ABI установленного runtime по SHA-256 закрепленного payload, а не по порядку обхода таблицы ABI, и выполнять при прогреве минимальный JavaScript smoke test, чтобы отклонять неработоспособный runtime до запуска пользовательских скриптов
* `Исправление` Отклонять заведомо несовместимый официальный runtime x86_64 до запуска процесса, если Android использует страницы больше 4 KiB, после локализации сбоя в 4 KiB page-size ceiling закрепленного JavaScriptCore, заменяя детерминированный abort Bun ограниченной диагностикой
* `Улучшение` Снизить минимальное системное требование: сохранить закрепленный официальный Android payload Bun 1.4.0 и смягчить нижнюю границу поддержки с Android 14 (API 34) до Android 13 (API 33), охватив больше устройств
* `Улучшение` Найти корневую причину неработоспособности на старых версиях: начиная с Android 13 системный seccomp разрешает вызываемый Bun raw syscall `close_range`, а сбой на реальном устройстве API 31 доказывает, что для API 28 по 32 нужно патчить сам Bun и одними изменениями manifest это не решить
* `Улучшение` Заложить основу будущей поддержки Android 9+: создать точно воспроизводимую серию патчей исходного кода Bun (6 патчей) и зафиксировать входные данные сборки (закрепленные NDK и контейнер, 22 активные зависимости Android release); эта работа создаёт отдельную экспериментальную ветку и не изменяет официальный runtime в текущих пакетах
* `Улучшение` Усилить проверки качества пакетов: каждый Debug и Release APK проверяется на 16 KB ZIP alignment, точное содержимое ABI, размер и SHA-256 закрепленного payload Bun, а установленные байты payload сверяются на тестовом устройстве Android 13
* `Улучшение` Укрепить цепочку поставок: зафиксировать точные байты 19 архивов исходного кода Bun и 17 загрузок toolchain, учесть 181 запись целостности Cargo и 172 записи реестра Bun, а также добавить отклоняющий перезапись materializer и предпроверку сборки двух ABI под защитой шлюза `buildReady`
* `Улучшение` Расширить библиотеку готовых к копированию и запуску примеров: добавить комментированные сетевые запросы fetch, файловый ввод-вывод в приватном рабочем каталоге, потоковые stdout/stderr и более содержательные типы TypeScript; документационный шлюз проверяет директиву `"bun";` в первой строке и ограничения одного исходного файла без установки пакетов
* `Улучшение` Проверить выполнение с 16 KB на AVD Android 16 (API 36) с обязательным PAGE_SIZE=16384: одно-ABI APK `arm64-v8a` проходит все 5 инструментальных тестов Binder через `libndk_translation`, а нативный payload `x86_64` завершается с кодом 134 даже на минимальном скрипте; поэтому общая поддержка 16 KB не заявляется
* `Улучшение` Закрыть Cargo-часть экспериментальной цепочки сборки для Android 9+: зафиксировать и материализовать все 181 архив crates.io (26 354 160 байт), создать directory source с проверяемыми контрольными суммами файлов и доказать, что закреплённый Cargo загружает весь workspace Bun с `--locked --offline` и пустым `CARGO_HOME`; этот результат охватывает только входы Cargo и сам по себе не замыкает остальные входы сборки
* `Улучшение` Закрыть часть этой цепочки, связанную с реестром Bun: свести 172 ссылки lock к 125 уникальным npm-архивам для Linux x64 (31 498 870 байт), пересобрать минимальный cache из зафиксированных tarball и выполнить все три frozen install в закреплённом контейнере Ubuntu с отключённой сетью и cache только для чтения; `esbuild@0.21.5` — единственная зависимость с доверенным postinstall
* `Улучшение` Завершить шлюз воспроизводимой сборки исправленного runtime без его поставки: зафиксировать 155 host-архивов `.deb` (422 223 096 байт) в воспроизводимом OCI-образе, расширить замыкание Cargo до 206 уникальных архивов, дважды чисто и без сети собрать обе 64-битные ABI с побайтово идентичным результатом и закрепить ELF-аудит на чистом Node; прямые shell-пробы на API 28 и 31 проходят, а шлюзы APK и процесса приложения остаются открыты
* `Улучшение` Реализовать проверяемые assets соответствующих исходников для Release: отделить исходники от APK, сохранив их в одном Release; упаковать точные исходники Bun/WebKit/JSC, 19 native-, 206 Cargo- и 125 npm-архивов, patches, инструкции build/relink и публичное уведомление о лицензиях; делить крупные assets по 1,9 GB, связать байты APK/runtime/source машиночитаемым manifest и SHA256SUMS и публиковать draft только после совпадения всех GitHub SHA-256; это фиксирует автоматизированную техническую проверку, а не юридическое одобрение
* `Зависимость` Добавить Kotlin Parcelize runtime, чтобы R8 в Release сохранял общий класс контракта Parcelable

# v0.1.0

###### 2026/09/01

* `Подсказка` Первый выпуск: каждый запуск выполняет один отдельный файл скрипта; встроенные функции AutoJs6, мост Java, многофайловые проекты и относительные импорты пока недоступны
* `Функция` Добавить отдельный движок `bun`: поместите `"bun";` в первую строку скрипта, чтобы выполнять JavaScript и TypeScript официальным Android executable Bun 1.4.0; фактическая команда `bun run --no-install <source>`, зависимости никогда не устанавливаются автоматически
* `Функция` Возвращать вывод запуска в реальном времени: stdout и stderr передаются ограниченными порциями через oneway Binder callback, а итоговый результат сообщает только статус и диагностику без полного потока вывода
* `Функция` Держать запуск под контролем: скрипты выполняются в изолированном процессе плагина `:bun_runtime` с явной отменой, timeout по умолчанию 60 секунд, запросом информации runtime и prewarming
* `Функция` Предоставить официальные 64-bit Android payload для `arm64-v8a` и baseline `x86_64`, а также пакеты для одного ABI и `universal`
* `Функция` Обеспечить полный опыт плагина: обнаружение плагина, защищенную разрешением активацию (Wake), полные метаданные PluginInfo и документацию пользователя на 10 языках
* `Улучшение` Принять версионированный Binder contract с передачей исходного кода через ParcelFileDescriptor, лимитом исходного кода 16 MiB и лимитом суммарного вывода 8 MiB
* `Улучшение` Запускать Bun из read-only native library directory Android и проверять размер, SHA-256 и свойства ELF закрепленных release archive и упакованных binary
* `Улучшение` Проверить, что PT_LOAD alignment обоих упакованных executable составляет не менее 16 KB, и честно задокументировать, что тестирование в реальной 16 KB среде Android еще не завершено
* `Улучшение` Генерировать README, инструкции центра плагинов и встроенный changelog из проверенных источников JSON, добавив CI-проверки сборки, Markdown и артефактов runtime
* `Улучшение` Установить предварительный минимум Android 14 (API 34): на реальном устройстве API 31 syscall `close_range` Bun завершается seccomp сигналом `SIGSYS`, одно устройство Sony с API 33 неожиданно прошло тест, но это не доказывает переносимость, а JS- и TS-тесты полного цикла через Binder на реальном устройстве API 35 пройдены
